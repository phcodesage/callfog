# Base URL of the Callfog backend (Caddy in front of LiveKit + the token service).
# The token service returns the LiveKit WebSocket URL, so this is the only setting the app needs.
NEXT_PUBLIC_CALLFOG_API_URL=https://your-backend-host.example.com
